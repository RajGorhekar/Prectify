<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.prectify">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Faclogin"></activity>
        <activity android:name=".Userprofile" />
        <activity android:name=".DetailActivity" />
        <activity
            android:name=".MainActivity"
            android:label="Prectify"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".RaiseQuery" />
        <activity android:name=".QueryType" />
        <activity android:name=".StuRegister" />
        <activity android:name=".StuLogin" />
        <activity android:name=".SelectType" />
        <activity android:name=".Splashpage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>